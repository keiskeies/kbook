# KBook 优化计划

> 基于本轮重构对话整理，待审阅后执行

---

## 一、本轮已完成的核心重构

### 1.1 后端新增 API

| API | 路径 | 说明 |
|-----|------|------|
| 匹配度详情 | `GET /api/recommend/match-detail/{bookId}` | 返回各维度匹配分数（年龄、性别、MBTI、职业等） |
| 3分钟速读 | `GET /api/books/{id}/speed-read` | AI 生成结构化摘要（核心观点/适合谁/不适合谁/收获/难度） |

### 1.2 前端页面重构

| 页面 | 改动 |
|------|------|
| 首页 | 「为你推荐」提升为第一模块，合并「高分佳作/新书速递/热门榜单」为「发现」Tab，阅读统计移到底部 |
| 书籍详情页 | 新增匹配度卡片、3分钟速读卡片、AI 预设问题入口 |

### 1.3 算法修复

- 匹配度分数归一化（sigmoid 函数，解决超过 100% 问题）
- 匹配度详情与批量接口数据一致性修复
- 速读缓存策略（首次 AI 生成后持久化，维度得分变化时自动清除）

---

## 二、待优化：页面美化

### 2.1 首页视觉升级

#### 现状问题
- 模块堆砌感强，缺乏视觉层次
- 「为你推荐」和「发现」之间没有视觉区分
- 卡片样式单一，缺乏品牌辨识度

#### 优化方向
- ** hero 区域**：在「为你推荐」上方增加个性化问候语（"你好，INTJ 技术人，今天想探索什么？"）
- **推荐卡片**：增加匹配度徽章（圆形进度环或百分比标签），高匹配度书籍增加「强烈推荐」角标
- **发现 Tab**：增加图标化 Tab（🏆 高分 / ⚡ 新书 / 🔥 热门），当前选中 Tab 增加下划线动画
- **背景层次**：推荐区域用浅色渐变背景，发现区域用纯白背景，形成视觉分区
- **空状态美化**：书架为空时显示插画 + 引导文案 + 「去书城逛逛」按钮

### 2.2 书籍详情页视觉升级

#### 现状问题
- 信息密度过高，缺乏呼吸感
- 匹配度卡片和速读卡片样式简陋
- AI 问答入口不够醒目

#### 优化方向
- **封面区域**：增加毛玻璃效果背景（提取封面主色调）
- **匹配度卡片**：
  - 总体匹配度用大号圆形进度环展示（如 92%）
  - 各维度用彩色进度条（高匹配绿色、中匹配橙色、低匹配灰色）
  - 增加「为什么推荐给我」按钮，点击展开匹配逻辑说明
- **3分钟速读卡片**：
  - 核心观点用引用块样式（左侧彩色竖线）
  - 「适合谁」用绿色标签，「不适合谁」用红色标签
  - 难度标签用徽章样式（入门=绿/中等=橙/进阶=红）
- **AI 问答入口**：
  - 增加「💬 向 AI 提问」悬浮按钮（固定在右下角）
  - 预设问题用胶囊按钮样式，hover 时有轻微上浮动画

### 2.3 全局设计系统建设

#### 现状问题
- 没有统一的设计 token
- 间距、圆角、阴影全凭手感
- 暗色模式切换生硬

#### 优化方向
- **定义设计 token**：
  - 颜色：主色、辅色、成功、警告、错误、中性色阶
  - 间距：4px 基数（4/8/12/16/24/32/48）
  - 圆角：sm(4px)/md(8px)/lg(12px)/xl(16px)/full
  - 阴影：sm/md/lg 三级
- **组件规范化**：
  - 卡片统一用 `bg-card border border-border/50 rounded-2xl shadow-sm`
  - 按钮统一用 `rounded-lg` 而非混用 `rounded-md`/`rounded-xl`
  - 图标统一用 `h-4 w-4` 或 `h-5 w-5`，不要混用
- **动画规范**：
  - 页面切换用 `fade` 或 `slide`
  - 卡片 hover 用 `scale-[1.02]` + `shadow-md` 过渡
  - 加载骨架屏用 `animate-pulse` 统一

---

## 三、待优化：用户体验优化

### 3.1 Onboarding 流程

#### 现状问题
- 新用户注册后没有引导填写画像
- 没有画像的用户看到的推荐是随机的
- 用户不知道「画像」是什么，也不知道怎么填

#### 优化方向
- **渐进式画像收集**：
  - 注册后第一步：选择 3 个核心标签（年龄区间、MBTI、职业）
  - 使用过程中的微交互：「你对这本书的推荐满意吗？」→ 不满意则引导调整画像
  - 个人中心增加「画像雷达图」，直观展示当前画像
- **画像价值说明**：
  - 在推荐区域增加小字「基于你的画像推荐」，hover 显示「什么是画像？」tooltip
  - 首次看到匹配度时，增加「这个数字代表什么？」引导

### 3.2 加载与空状态

#### 现状问题
- 全局 loading 只有一个 spinner
- 空状态只有「暂无数据」四个字
- 网络错误没有重试机制

#### 优化方向
- **骨架屏**：
  - 首页每个模块独立骨架屏（推荐卡片骨架、发现列表骨架、标签骨架）
  - 详情页独立骨架屏（封面骨架、匹配度骨架、速读骨架）
- **空状态**：
  - 书架为空：插画 + 「你的书架空空如也」+ 「去书城逛逛」按钮
  - 推荐为空：「完善画像后，我们会为你推荐更适合的书」+ 「去完善画像」按钮
  - 搜索无结果：「没有找到相关书籍，试试其他关键词？」+ 热门搜索推荐
- **错误处理**：
  - 网络错误：toast 提示 + 「重试」按钮
  - AI 服务超时：「AI 服务暂时不可用，请稍后重试」+ 降级显示书籍简介
  - 服务器 500：「服务开小差了，请刷新页面」+ 刷新按钮

### 3.3 交互细节

#### 现状问题
- 书籍卡片点击区域不明确
- 没有长按操作（移动端）
- 返回上一页时滚动位置丢失

#### 优化方向
- **卡片交互**：
  - 书籍卡片增加 hover 效果（轻微上浮 + 阴影加深）
  - 移动端支持长按：长按书籍卡片弹出菜单（加入书架/分享/不感兴趣）
  - 点击书籍封面放大预览（ImageViewer 优化）
- **滚动恢复**：
  - 首页滚动位置在返回时恢复
  - 发现 Tab 切换时保持当前 Tab 的滚动位置
- **手势支持**：
  - 阅读器支持左右滑翻页
  - 详情页支持右滑返回

### 3.4 阅读体验优化

#### 现状问题
- 阅读器设置入口隐蔽
- 没有阅读进度可视化
- 阅读过程中无法快速调起 AI

#### 优化方向
- **阅读进度**：
  - 顶部增加细进度条（当前章节 / 全书进度）
  - 章节切换时显示「第 X 章 / 共 Y 章」
- **AI 辅助阅读**：
  - 长按文本弹出菜单：「AI 解释这段」「AI 总结本章」「添加到笔记」
  - 阅读器右下角增加「问 AI」悬浮按钮
- **设置优化**：
  - 字体大小用滑块调节，实时预览
  - 主题切换增加「跟随系统」选项

---

## 四、待优化：核心功能加强

### 4.1 推荐系统性能优化

#### 现状问题
- `RecommendService.generateWithProgress()` 使用 `bookRepository.findAll()` 加载所有书籍
- 书籍数量增长后内存和 CPU 压力急剧上升
- 推荐计算没有预热机制

#### 优化方向
- **向量预过滤**：
  - 将用户画像编码为向量，在 Qdrant 中先召回 Top 100
  - 只对这 100 本书进行精细的 `RecommendMatchCalculator` 计算
- **分层缓存**：
  - L1: Redis 缓存用户画像 → 推荐结果（1 小时 TTL）
  - L2: 本地缓存书籍 relevanceScores（常驻）
  - L3: 数据库（兜底）
- **异步预计算**：
  - 用户画像变更时，异步触发推荐重算
  - 用户打开 App 时直接读取缓存
- **增量更新**：
  - 新书入库时，只计算新书的匹配分，而非全量重算
  - 用户画像微调时，只重新排序，不复算所有分数

### 4.2 AI 深度阅读强化

#### 现状问题
- 3分钟速读内容固定，无法根据用户画像定制
- AI 问答预设问题质量参差不齐
- 没有「这本书 vs 那本书」的对比能力

#### 优化方向
- **画像定制速读**：
  - 速读摘要中增加「为什么适合你」段落（基于用户画像维度）
  - 例如：「作为 INTJ 技术从业者，这本书的系统思维方法论特别适合你」
- **智能预设问题**：
  - 基于用户画像生成个性化问题
  - 例如 INTJ 用户看到：「这本书对 INTJ 有什么特别的价值？」
  - 例如创业者看到：「书中有哪些适合创业初期的经验？」
- **书籍对比**：
  - 支持选择两本书进行 AI 对比
  - 对比维度：核心观点差异、适合人群差异、难度差异、互补性

### 4.3 社交与发现

#### 现状问题
- 社交功能薄弱，只有基础评论
- 榜单是简单的排序，没有个性化
- 没有「朋友在读什么」的社交发现

#### 优化方向
- **评论增强**：
  - 支持书评长文（Markdown 格式）
  - 支持「划线分享」（选中段落 + 评论生成分享卡片）
  - 评论支持点赞、回复、收藏
- **个性化榜单**：
  - 「本周最受 INTJ 欢迎的书」
  - 「技术从业者最近在读什么」
  - 「30-39 岁已婚有娃的人在读什么」
- **社交发现**：
  - 关注感兴趣的用户，查看他们的书架和书评
  - 「和你画像相似的人在读什么」

### 4.4 数据驱动优化

#### 现状问题
- 没有埋点，无法分析用户行为
- 推荐效果无法量化评估
- A/B 测试基础设施缺失

#### 优化方向
- **埋点体系**：
  - 页面浏览：首页、详情页、阅读器、个人中心
  - 点击事件：推荐点击、书架操作、AI 问答、分享
  - 转化漏斗：打开 App → 看到推荐 → 点击书籍 → 查看速读 → 开始阅读
- **推荐评估**：
  - 点击率（CTR）：推荐书籍的点击比例
  - 阅读完成率：点击后实际阅读的比例
  - 用户反馈：「推荐准确」/「不感兴趣」按钮
- **A/B 测试**：
  - 测试不同推荐算法的效果
  - 测试不同 UI 布局的转化率
  - 测试不同速读内容长度的留存率

---

## 五、技术债务清理

### 5.1 前端

| 问题 | 优先级 | 行动 |
|------|--------|------|
| 277 个 ESLint 错误 | P0 | 修复 `any` 类型、React Hooks 违规 |
| TypeScript 编译错误 | P0 | 修复 `BookChatSheetProps` 缺少 `initialQuestion` |
| 死代码清理 | P1 | 删除未使用的 import 和变量 |
| 测试覆盖 | P2 | 补充单元测试和 E2E 测试 |

### 5.2 后端

| 问题 | 优先级 | 行动 |
|------|--------|------|
| `findAll()` 全量加载 | P0 | 改为分页或向量预过滤 |
| 重复代码提取 | P1 | `RecommendService` / `RecommendComputeService` 公共方法提取 |
| 空 catch 块 | P1 | 添加日志记录 |
| 循环依赖根治 | P2 | 模块拆分或事件驱动重构 |
| 日志级别规范 | P2 | info → debug 调整 |

---

## 六、执行优先级

### Phase 1（本周）：修复阻塞问题
- [ ] 修复前端 ESLint 错误和 TypeScript 编译错误
- [ ] 修复后端 `findAll()` 性能问题
- [ ] 修复空 catch 块

### Phase 2（下周）：核心体验优化
- [ ] 首页视觉升级（hero 区域、推荐卡片、发现 Tab）
- [ ] 详情页视觉升级（匹配度进度环、速读卡片美化）
- [ ] Onboarding 流程优化

### Phase 3（下下周）：功能强化
- [ ] AI 画像定制速读
- [ ] 智能预设问题
- [ ] 社交评论增强

### Phase 4（持续）：数据驱动
- [ ] 埋点体系搭建
- [ ] 推荐效果评估
- [ ] A/B 测试基础设施

---

## 七、设计稿参考

### 首页布局

```
┌─────────────────────────────┐
│  👤 你好，INTJ 技术人         │  ← hero 区域（个性化问候）
│  今天想探索什么？              │
├─────────────────────────────┤
│  🔍 搜索书名/作者/主题         │
├─────────────────────────────┤
│  📚 为你推荐（基于你的画像）    │  ← 第一优先级模块
│  ┌─────┐ ┌─────┐ ┌─────┐   │
│  │ 书1  │ │ 书2  │ │ 书3  │   │
│  │ 92% │ │ 87% │ │ 85% │   │  ← 匹配度徽章
│  └─────┘ └─────┘ └─────┘   │
│  [查看全部推荐]               │
├─────────────────────────────┤
│  🧭 发现                      │  ← 合并 Tab
│  [🏆 高分] [⚡ 新书] [🔥 热门] │
│  ┌─────┐ ┌─────┐ ┌─────┐   │
│  │ 书4  │ │ 书5  │ │ 书6  │   │
│  └─────┘ └─────┘ └─────┘   │
├─────────────────────────────┤
│  🏷️ 热门标签                  │
├─────────────────────────────┤
│  📊 阅读统计（弱化）           │  ← 移到底部
└─────────────────────────────┘
```

### 书籍详情页布局

```
┌─────────────────────────────┐
│  ← 返回                      │
├─────────────────────────────┤
│  [封面] 书名                 │  ← 毛玻璃背景
│  作者 · 格式 · 评分           │
├─────────────────────────────┤
│  📊 匹配度: 92%              │  ← 圆形进度环
│  ┌─────────────────────────┐│
│  │ ✅ INTJ 匹配度: 95%     ││  ← 彩色进度条
│  │ ✅ 技术从业者: 88%      ││
│  │ ✅ 30-39岁: 82%         ││
│  └─────────────────────────┘│
├─────────────────────────────┤
│  ⏱️ 3 分钟速读               │
│  ┌─────────────────────────┐│
│  │ 📖 核心观点              ││  ← 引用块样式
│  │ 1. ...                  ││
│  │ 2. ...                  ││
│  │                         ││
│  │ 🟢 适合谁                ││  ← 绿色标签
│  │ 🔴 不适合谁              ││  ← 红色标签
│  │                         ││
│  │ 📈 难度: 中等            ││  ← 徽章样式
│  └─────────────────────────┘│
├─────────────────────────────┤
│  💬 AI 深度问答              │  ← 预设问题胶囊按钮
│  [问题1] [问题2] [问题3]    │
├─────────────────────────────┤
│  📝 评论 (15)                │
├─────────────────────────────┤
│  [开始阅读] [加入书架] [分享] │
└─────────────────────────────┘
│  💬                        │  ← AI 悬浮按钮
└─────────────────────────────┘
```

---

> 本计划基于本轮重构对话整理，涵盖了页面美化、用户体验优化、核心功能加强三个维度。建议按 Phase 顺序执行，每个 Phase 完成后进行用户测试和数据验证。
