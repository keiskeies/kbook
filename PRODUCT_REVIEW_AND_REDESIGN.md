# KBook 产品评审与重构方案

> 评审人视角：大厂产品经理 + 设计师
> 日期：2026-06-02
> 范围：产品定位、信息架构、页面设计、操作流程、工程规范

---

## 目录

1. [总体评价](#一总体评价)
2. [产品定位修正](#二产品定位修正)
3. [信息架构重构](#三信息架构重构)
4. [页面设计详案](#四页面设计详案)
   - [4.1 首页](#41-首页)
   - [4.2 书籍详情页](#42-书籍详情页)
   - [4.3 阅读器](#43-阅读器)
   - [4.4 AI 对话页](#44-ai-对话页)
   - [4.5 书架页](#45-书架页)
   - [4.6 个人中心](#46-个人中心)
   - [4.7 注册 / Onboarding](#47-注册--onboarding)
5. [核心操作流程](#五核心操作流程)
   - [5.1 新用户首次体验](#51-新用户首次体验)
   - [5.2 找书→决策→阅读](#52-找书决策阅读)
   - [5.3 AI 深度阅读](#53-ai-深度阅读)
   - [5.4 阅读→反馈→推荐迭代](#54-阅读反馈推荐迭代)
6. [工程改进清单](#六工程改进清单)
7. [执行路线图](#七执行路线图)

---

## 一、总体评价

KBook 的核心方向——「AI + 深度阅读」——是正确的。三分钟速读、AI 问答、匹配度推荐这三个功能构成了与微信读书/得到可区分的差异点。

但当前的产品实现有三个根本性问题：

| 问题 | 症状 | 根因 |
|------|------|------|
| **信息过载** | 首页 8 个模块，详情页信息密度堪比 Bloomberg | 没有做信息优先级排序，什么功能都想要 |
| **缺乏分层** | 1266 行的 detail.tsx，692 行的 BookChatSheet | 组件不拆分，逻辑不抽象，UI 和状态全糊在一起 |
| **沉默失败** | 35+ 处 `catch { /* ignore */ }`，零测试 | 没有错误处理策略，没有质量保障体系 |

下面的方案针对这三个问题，给出具体的页面设计、操作流程和组件拆分方案。

---

## 二、产品定位修正

### 现状

> 「AI 阅读平台」——过于宽泛，没有回答"比微信读书好在哪"

### 修正后

> **KBook = AI 帮你读，你只读好的**

一句话解释：用户面对一本书，KBook 用 AI 回答三个问题——

1. **这本书值不值得读？**（三分钟速读 → 决策）
2. **这本书对我有什么价值？**（匹配度分析 → 个性化）
3. **读的时候遇到不懂的怎么办？**（AI 深度问答 → 辅助）

这三个问题对应三个核心功能，也是唯一需要在产品中突出展示的三个功能。其他一切（榜单、社交、统计）都是辅助。

### 用户画像字段：保留现有，优化粒度

**验证结论**：`RecommendMatchCalculator` 对以下 10 个维度全部有消费——不是摆设。维度得分由 AI 在书籍入库时逐书打分，算法层加权求和。无效维度的得分天然趋近于 0，因此保留更多维度不会引入噪声。

| 字段 | 算法权重 | 当前状态 | 优化方向 |
|------|---------|---------|---------|
| birthday → 年龄组 | 1.5 | ✅ 合理（自动计算年龄，无需手动维护） | 保持 |
| MBTI | 1.3 | ✅ 已有相邻类型衰减 | 保持 |
| 性别 | 1.0 | ✅ 已有反向惩罚 | 保持 |
| 婚姻 | 1.0 | ⚠️ 全局 1.0 权重偏高 | 降到 0.6，或按品类条件激活（技术书已婚/未婚得分一致时 dev 趋近 0，自然消解） |
| 职业 | 1.0 | ✅ 支持多选 + 相邻衰减 | 保持 |
| 学历 | 0.8 | ⚠️ 字段名为 education 但前端 label 是「期望学历」 | 统一为 aspiration_education 语义 |
| 心情 | 0.7 | ⚠️ 已支持随时修改，但入口藏在个人中心 | 首页增加快捷切换入口，降低修改成本 |
| 创业 | 0.6 | ✅ | 保持 |
| 年收入 | 0.5 | ⚠️ 字段名为 annualIncome 但前端 label 是「期望年收入」 | 改为 aspirationIncome，统一为 aspiration 而非 demographic |
| 是否有孩子 | 1.0 | ❌ boolean 太粗糙 | **改为孩子年龄区间（多选）**：`0-2岁`/`3-6岁`/`7-12岁`/`13-17岁`/`18+岁`/`无孩子`，算法用邻近衰减 |

**覆盖率系数**：当前设计是"填 N 个维度 → 得分 × 系数"（1维×0.42, 10维×1.0）。这惩罚了不完整画像。但反过来说，它也驱动用户完善画像——如果用户发现推荐不准，自然会去补字段。权衡后保持，但将最低系数从 0.42 提升到 0.6，让新用户的推荐不至于完全随机。

**Onboarding 收集策略**：
- 注册时不收集任何画像信息（只收邮箱+密码+验证码）——降低注册摩擦
- 注册完成后弹出欢迎引导（可跳过）：年龄 + MBTI + 职业 + 孩子（仅四项，其余在个人中心补充）
- 首页推荐区域旁常驻「完善画像」入口，点击进入个人资料编辑页
- 心情字段在首页有快捷切换入口（一组 emoji 横滑条），随时可改

---

## 三、信息架构重构

### 3.1 TabBar 导航（5 个 Tab → 4 个）

```
当前：
首页 | 榜单 | [AI 大按钮] | 书架 | 我的

改为：
首页 | 书架 | [AI 大按钮] | 我的
```

**删除「榜单」**的理由：
- 榜单是内容运营工具，不是用户每日必用功能
- 榜单内容并入首页「发现」Tab，减少一个一级入口
- 减少认知负荷（4 个 Tab 是移动端最佳数量）

### 3.2 页面层级

```
AppLayout（有 TabBar）
├── 首页 /home
├── 书架 /bookshelf
├── AI 对话 /ai
├── 我的 /profile
│   ├── 个人资料编辑
│   ├── 阅读记录
│   ├── 阅读统计
│   ├── 我的评论
│   └── 关注/粉丝
└── （榜单内容迁入首页发现 Tab）

BlankLayout（全屏，无 TabBar）
├── 书籍详情 /book/:id
├── 阅读器 /reader/:id
├── 搜索 /search
├── 登录 /login
├── 注册 /register
├── Onboarding /onboarding
├── 通知 /notifications
├── 聊天 /chat, /chat/:id
├── 管理后台 /admin/*
└── 404
```

### 3.3 信息层级原则

每条页面遵循**一个核心行动点**原则：

| 页面 | 核心行动点 | 次要行动 | 禁止出现 |
|------|-----------|----------|---------|
| 首页 | 点击推荐书籍进入详情 | 搜索、查看继续阅读 | 阅读统计、热门标签 |
| 详情页 | 查看三分钟速读 → 决定是否阅读 | 看评论、AI 问答 | 文件大小、格式信息放在底部 |
| 阅读器 | 专注阅读 | 调设置、TOC、AI 问答 | 任何非阅读相关的 UI |
| AI 对话 | 和 AI 聊书 | 切换会话、查看历史 | 广告、推荐 |

---

## 四、页面设计详案

### 4.1 首页

#### 信息层级（从上到下，共 4 个模块）

```
┌─────────────────────────────────┐
│  📖 KBook          🔔           │  ← 顶栏（固定）
├─────────────────────────────────┤
│                                 │
│  下午好，INTJ 技术人              │  ← Hero（个性化问候）
│  今天想探索什么？                 │
│                                 │
│  ┌─────────────────────────┐    │
│  │ 🔍 搜索书名、作者或主题   │    │  ← 搜索入口（伪输入框，点击跳 /search）
│  └─────────────────────────┘    │
├─────────────────────────────────┤
│                                 │
│  继续阅读                       │  ← 模块 1（有数据时才显示）
│  ┌──────┐ ┌──────┐ ┌──────┐   │
│  │ 封面  │ │ 封面  │ │ 封面  │   │     横向滚动，最多 6 本
│  │ 45%  │ │ 72%  │ │ 12%  │   │     每张卡片：封面 + 书名 + 进度条
│  └──────┘ └──────┘ └──────┘   │
│                                 │
├─────────────────────────────────┤
│                                 │
│  为你推荐    基于你的画像 ⓘ      │  ← 模块 2（核心模块，有数据时显示）
│  ┌─────────────────────────┐    │
│  │ 📕 书名           92% 匹配│    │     竖向列表，最多 10 本
│  │    作者 · 标签 · 标签    │    │     每行：小封面 + 书名 + 作者 + 匹配度徽章
│  └─────────────────────────┘    │
│  ┌─────────────────────────┐    │
│  │ 📕 ...                  │    │
│  └─────────────────────────┘    │
│  [查看全部推荐 →]               │
│                                 │
│  （无数据时显示引导卡片：        │
│   "完善画像后获取个性化推荐"     │
│   [去完善画像]                  │
│  ）                             │
│                                 │
├─────────────────────────────────┤
│                                 │
│  发现                          │  ← 模块 3
│  [高分佳作] [新书速递] [热门]    │     三个 Tab，默认「高分佳作」
│  ┌──────┐ ┌──────┐            │     双列网格，每列：封面 + 书名 + 评分
│  │ 封面  │ │ 封面  │            │
│  │ 书名  │ │ 书名  │            │
│  │ ⭐4.8 │ │ ⭐4.6 │            │
│  └──────┘ └──────┘            │
│  ┌──────┐ ┌──────┐            │
│  │ ...  │ │ ...  │            │
│  └──────┘ └──────┘            │
│                                 │
│  （上拉加载更多）                │
│                                 │
├─────────────────────────────────┤
│  [TabBar]                       │  ← 底栏
└─────────────────────────────────┘
```

#### 关键变化

| 变化 | 原来 | 现在 | 原因 |
|------|------|------|------|
| 模块数量 | 8 个 | 4 个（继续阅读/为你推荐/发现/底栏） | 减少决策疲劳 |
| 阅读统计 | 首页首屏 | 移到个人中心 | 不是首页核心任务 |
| 热门标签 | 独立模块 | 整合到发现 Tab | 减少视觉噪音 |
| 为你推荐空状态 | 无 | 引导卡片 | 推动画像完善 |
| 3D 轮播 | 有 | 改为普通横向滚动 | 3D 效果增加渲染成本且不符合内容优先原则 |

#### 组件拆分

```
pages/home/index.tsx                    ← 页面编排（~80 行）
components/home/HeroSection.tsx          ← 问候 + 搜索入口
components/home/ContinueReading.tsx      ← 继续阅读横向滚动
components/home/PersonalizedList.tsx     ← 为你推荐竖向列表
components/home/PersonalizedEmpty.tsx    ← 推荐为空引导卡片
components/home/DiscoverTabs.tsx         ← 发现 Tab 切换 + 双列网格
components/home/DiscoverItem.tsx         ← 发现列表单项
```

每个组件 ≤ 150 行。

---

### 4.2 书籍详情页

#### 信息层级（从上到下）

```
┌─────────────────────────────────┐
│  ← 返回         ··· 更多操作     │  ← 顶栏（固定）
├─────────────────────────────────┤
│                                 │
│       ┌───────────┐            │  ← 封面区
│       │           │            │     大封面（160×213px）
│       │   封面    │            │     下方：书名、作者、评分、格式标签
│       │           │            │
│       └───────────┘            │
│       书名                      │
│       作者 · EPUB · ⭐4.6       │
│                                 │
│  ┌─────────────────────────┐    │
│  │ [开始阅读]  [加入书架]    │    │  ← 操作按钮行（两个主按钮）
│  └─────────────────────────┘    │
│                                 │
├─────────────────────────────────┤
│                                 │
│  ⏱️ 三分钟速读        AI 生成    │  ← ⭐ 核心模块（首屏主体，默认展开）
│  ┌─────────────────────────┐    │
│  │                         │    │
│  │ 📖 核心观点              │    │     引用块样式，左侧绿色竖线
│  │ 1. 第一条核心观点        │    │
│  │ 2. 第二条核心观点        │    │
│  │ 3. 第三条核心观点        │    │
│  │                         │    │
│  │ 🟢 适合谁                │    │     绿色标签组
│  │ 标签1 标签2 标签3        │    │
│  │                         │    │
│  │ 🔴 不适合谁              │    │     红色标签组
│  │ 标签1 标签2              │    │
│  │                         │    │
│  │ 📈 你将收获              │    │     要点列表
│  │ • 收获 1                 │    │
│  │ • 收获 2                 │    │
│  │                         │    │
│  │ 📊 难度：中等            │    │     徽章：入门(绿)/中等(橙)/进阶(红)
│  │                         │    │
│  │ 💡 为什么适合你（个性化）  │    │     基于用户画像，如果用户已登录且有画像
│  │ 作为 INTJ 技术从业者...   │    │
│  │                         │    │
│  └─────────────────────────┘    │
│                                 │
├─────────────────────────────────┤
│                                 │
│  📊 匹配度分析     92% 总体匹配   │  ← 匹配度模块（可折叠，默认折叠）
│  ┌─────────────────────────┐    │
│  │ [展开后显示]             │    │     大圆形进度环（92%）
│  │ 各维度进度条：            │    │     各维度彩色进度条
│  │ MBTI 匹配     ████████ 95%│    │
│  │ 职业匹配      ███████ 88% │    │
│  │ 年龄匹配      ██████ 82%  │    │
│  └─────────────────────────┘    │
│                                 │
├─────────────────────────────────┤
│                                 │
│  📝 简介                       │  ← 书籍简介（可展开）
│  这是一本关于...                │
│  [展开更多]                     │
│                                 │
├─────────────────────────────────┤
│                                 │
│  💬 AI 深度问答                 │  ← AI 入口
│  [本书的核心方法论是什么？]       │     预设问题，胶囊按钮样式
│  [这本书对 INTJ 有什么价值？]     │     hover 上浮
│  [书中有哪些可实操的建议？]       │
│  [自定义提问...]                │     ← 点击打开 BookChatSheet
│                                 │
│  📌 右下角悬浮按钮：            │
│  ┌──┐                          │
│  │💬│  ← 始终可见的 AI 入口     │
│  └──┘                          │
│                                 │
├─────────────────────────────────┤
│                                 │
│  📝 评论 (15)                   │  ← 评论模块
│  ┌─────────────────────────┐    │
│  │ ...                      │    │
│  └─────────────────────────┘    │
│                                 │
├─────────────────────────────────┤
│                                 │
│  元信息                         │  ← 弱化展示
│  文件大小：2.3 MB · 格式：EPUB  │     灰色小字，底部
│                                 │
└─────────────────────────────────┘
```

#### 关键变化

| 变化 | 原来 | 现在 | 原因 |
|------|------|------|------|
| 速读位置 | 可折叠卡片，在匹配度下方 | 首屏主体，默认展开 | 核心差异化功能必须首屏可见 |
| 匹配度 | 默认展开 | 默认折叠 | 非核心决策信息，降低首屏密度 |
| AI 入口 | 仅在右上角 | 右上角 + 右下角悬浮 + 预设问题 | 多触点引导 AI 使用 |
| 操作按钮 | 多个按钮分散 | 「开始阅读」+「加入书架」并排 | 两个主行动，符合 F 型视觉扫描 |
| 元信息 | 和简介同级 | 页面底部灰色小字 | 用户不需要在决策时看文件大小 |

#### 组件拆分

```
pages/book/detail.tsx                    ← 页面编排（~120 行）
components/book/BookCoverSection.tsx      ← 封面 + 基本信息 + 操作按钮
components/book/SpeedReadCard.tsx         ← 三分钟速读卡片（⭐ 核心组件）
components/book/MatchScoreCard.tsx        ← 匹配度分析（可折叠）
components/book/BookDescription.tsx       ← 简介（可展开）
components/book/AiQuestionPresets.tsx     ← AI 预设问题
components/book/BookMetaInfo.tsx          ← 元信息（底部弱化）
components/book/BookChatSheet.tsx         ← AI 问答 Sheet（已有，重构）
```

每个组件 ≤ 200 行。

---

### 4.3 阅读器

#### 布局

```
┌─────────────────────────────────┐
│  ← 返回   第 3 章 / 共 12 章    │  ← 顶栏（轻点屏幕显示/隐藏）
│  ████████████░░░░░░  45%       │  ← 细进度条（在顶栏内）
├─────────────────────────────────┤
│                                 │
│                                 │
│         阅读内容区域              │  ← 核心区
│     （全屏沉浸，无干扰）          │
│                                 │
│                                 │
│                                 │
├─────────────────────────────────┤
│  [目录] [设置] [主题] [AI 问答]  │  ← 底栏（轻点显示，3 秒后自动隐藏）
└─────────────────────────────────┘

┌──┐
│💬│  ← 右下角 AI 悬浮按钮（始终可见，半透明）
└──┘

┌──────────────┐
│ 🔊 TTS 浮窗   │  ← TTS 播放时显示：
│ 书名 - 章节    │     可拖拽位置
│ ▶️ ⏸️ ⏩ ⏪   │     最小化可缩成圆形按钮
└──────────────┘
```

#### 工具栏详细功能

| 按钮 | 功能 | 交互 |
|------|------|------|
| 目录 | 打开 TOC 抽屉 | 点击当前章节自动跳转，当前章节高亮 |
| 设置 | 打开设置面板 | 字体大小滑块（实时预览）、行间距、页边距、翻页模式 |
| 主题 | 循环切换：日间 → 夜间 → 护眼 | 长按弹出全部主题选项 |
| AI 问答 | 打开 AI 对话 Sheet | 自动带入当前选中文字（如果有选中） |

#### 长按文本菜单（新增）

```
选中一段文字后弹出：

┌──────────────────┐
│ 📖 AI 解释这段    │  ← 调用 BookChat，prompt 带入选中文
│ 📝 AI 总结本章    │  ← 调用 BookChat，prompt 带入当前章节
│ ✏️ 复制           │
│ 💡 添加到笔记     │  ← 新增功能（MVP 可先不做）
│ 📤 分享划线       │  ← 新增功能（MVP 可先不做）
└──────────────────┘
```

#### 读取器统一抽象（架构改进）

当前三个独立 hook（`useTxtReader` / `useEpubReader` / `usePdfReader`）改为统一接口：

```typescript
// 所有格式读取器实现此接口
interface ReaderAdapter {
  // 渲染
  mount(container: HTMLElement): void
  unmount(): void
  // 导航
  goToPosition(position: string): void
  goPage(dir: 'next' | 'prev'): void
  // 信息
  getChapters(): Chapter[]
  getCurrentChapter(): Chapter | null
  getProgress(): { progress: number; currentPosition: string }
  // 事件
  onProgressChange(cb: (progress: number, position: string) => void): void
  onChapterChange(cb: (chapter: Chapter) => void): void
  // 设置
  applySettings(settings: ReaderSettings): void
  // TTS
  extractTextSegments(): Promise<string[]>
}
```

#### 组件拆分

```
pages/reader/index.tsx                     ← 页面编排（~80 行）
components/reader/ReaderToolbar.tsx         ← 顶栏 + 进度条
components/reader/ReaderBottomBar.tsx       ← 底栏（目录/设置/主题/AI）
components/reader/ReaderContainer.tsx       ← 阅读内容容器（根据 format 选择适配器）
components/reader/SettingsPanel.tsx         ← 设置面板（已有，需重构）
components/reader/TocPanel.tsx              ← 目录面板（已有，需重构）
components/reader/TtsFloatPlayer.tsx        ← TTS 浮窗（已有）
components/reader/TextSelectionMenu.tsx     ← 长按文本菜单（新增）
components/reader/AiFloatButton.tsx         ← AI 悬浮按钮
```

---

### 4.4 AI 对话页

#### 布局

```
┌─────────────────────────────────┐
│  ☰ 会话列表    AI 阅读助手       │  ← 顶栏
├─────────────────────────────────┤
│                                 │
│  （空状态）                      │
│                                 │
│       🤖                        │  ← 吉祥物
│   我是你的 AI 阅读助手            │
│                                 │
│  我可以帮你：                     │
│  • 解读难懂的段落                 │
│  • 总结一本书的核心观点            │
│  • 对比两本书的差异               │
│  • 推荐适合你的下一本书            │
│                                 │
│  💡 试试问我：                    │
│  ┌──────────────────────────┐   │
│  │ 《原子习惯》的核心方法论？  │   │  ← 热门问题卡片
│  └──────────────────────────┘   │
│  ┌──────────────────────────┐   │
│  │ 推荐 3 本提升认知的书      │   │
│  └──────────────────────────┘   │
│  ┌──────────────────────────┐   │
│  │ 怎样培养深度阅读的习惯？    │   │
│  └──────────────────────────┘   │
│                                 │
├─────────────────────────────────┤
│ （有消息后）                     │
│                                 │
│  ┌──────────────────────┐       │
│  │ 用户消息              │       │  右对齐，绿色气泡
│  └──────────────────────┘       │
│                                 │
│  ┌────────────────────────┐     │
│  │ 🤖 AI 回复              │     │  左对齐，灰色气泡
│  │                        │     │  Markdown 渲染
│  │ （思考过程可折叠）       │     │  代码块格式化
│  │  [▼ 思考过程]           │     │  书籍引用 → 可点击跳转
│  └────────────────────────┘     │
│                                 │
│  💡 你可能还想问：               │  ← 追问建议
│  [追问1] [追问2] [追问3]        │
│                                 │
├─────────────────────────────────┤
│  ┌─────────────────────────┐    │
│  │ 输入你的问题...      [📎] │    │  ← 输入区（固定底栏）
│  │                   [发送] │    │     📎 预留附件按钮
│  └─────────────────────────┘    │
└─────────────────────────────────┘
```

#### 书籍引用卡片（新增）

当 AI 回复中提到某本书时，自动渲染为可点击卡片：

```json
// AI 回复中的标记（后端在 prompt 中要求返回特定格式）
{
  "type": "book_reference",
  "bookId": 42,
  "title": "原子习惯",
  "reason": "这本书详细解释了习惯形成的四步模型"
}
```

渲染效果：

```
┌──────────────────────────┐
│ 📕 原子习惯               │  ← 点击跳转书籍详情
│ 这本书详细解释了习惯形成    │
│ 的四步模型 →              │
└──────────────────────────┘
```

#### 会话管理

左侧抽屉（点击顶栏 ☰ 或右滑）：

```
┌─────────────────────┐
│ 会话列表        [＋] │  ← 新建会话
├─────────────────────┤
│ 📌 读书方法论讨论     │  ← 置顶会话（如果有）
│    3 条消息 · 2h 前  │
├─────────────────────┤
│ 关于《原子习惯》的问答  │  ← 普通会话
│    12 条消息 · 昨天   │
├─────────────────────┤
│ 推荐技术类书籍       │
│    5 条消息 · 3 天前  │
├─────────────────────┤
│ ...                  │
└─────────────────────┘
```

每个会话卡片支持 **左滑删除** 和 **长按置顶**。

#### 组件拆分

```
pages/ai/index.tsx                       ← 页面编排（~80 行）
components/ai/ChatEmptyState.tsx          ← 空状态引导 + 热门问题
components/ai/ChatMessageList.tsx         ← 消息列表（虚拟滚动）
components/ai/ChatMessageBubble.tsx       ← 单条消息气泡
components/ai/ChatThinkingBlock.tsx       ← 思考过程折叠块
components/ai/ChatFollowUps.tsx           ← 追问建议
components/ai/ChatInputBar.tsx            ← 输入区（固定底栏）
components/ai/ChatSidebar.tsx             ← 会话列表抽屉
components/ai/ChatSessionItem.tsx         ← 单个会话卡片
components/ai/BookReferenceCard.tsx       ← 书籍引用卡片（新增）
```

---

### 4.5 书架页

#### 布局

```
┌─────────────────────────────────┐
│  我的书架                       │  ← 顶栏
├─────────────────────────────────┤
│                                 │
│  （空状态）                      │
│                                 │
│       📚                        │  ← 插画
│   书架还是空的                    │
│   去首页发现好书吧               │
│                                 │
│   [去发现好书]                   │  ← primary 按钮，跳转首页
│                                 │
├─────────────────────────────────┤
│ （有数据）                       │
│                                 │
│  ┌──────┐ ┌──────┐            │  ← 双列网格
│  │ 封面  │ │ 封面  │            │     封面宽高比 3:4
│  │      │ │      │            │     圆角 12px
│  │ 书名  │ │ 书名  │            │     书名 14px，最多两行
│  │ 作者  │ │ 作者  │            │     作者 12px，单行截断
│  │ ██░░  │ │ ████  │            │     进度条高 3px，在封面下方
│  │ 45%  │ │ 82%  │            │
│  └──────┘ └──────┘            │
│  ┌──────┐ ┌──────┐            │
│  │ ...  │ │ ...  │            │
│  └──────┘ └──────┘            │
│                                 │
│  （长按卡片弹出菜单）             │
│  ┌──────────────┐               │
│  │ 📖 继续阅读    │               │
│  │ 📝 查看详情    │               │
│  │ 🗑️ 移出书架    │               │
│  └──────────────┘               │
│                                 │
└─────────────────────────────────┘
```

#### 关键变化

| 变化 | 原来 | 现在 | 原因 |
|------|------|------|------|
| 列数 | 三列 (grid-cols-3) | 两列 (grid-cols-2) | 封面更大，视觉更舒适 |
| 评分徽章 | 显示 | 移除 | 书架场景不需要，用户已经知道这些书 |
| 匹配度徽章 | 显示 | 移除 | 同上 |
| 空状态 | "暂无数据" | 插画 + 引导按钮 | 推动用户去发现书 |
| 长按菜单 | 无 | 有 | 移动端标准交互 |

#### 组件拆分

```
pages/bookshelf/index.tsx               ← 页面编排（~60 行）
components/bookshelf/BookGrid.tsx        ← 双列网格
components/bookshelf/BookGridItem.tsx    ← 单本书卡片
components/bookshelf/BookContextMenu.tsx ← 长按菜单
components/bookshelf/EmptyBookshelf.tsx  ← 空状态
```

---

### 4.6 个人中心

#### 布局

```
┌─────────────────────────────────┐
│  我的                           │  ← 顶栏
├─────────────────────────────────┤
│                                 │
│  ┌────┐                         │
│  │头像│ 昵称                     │  ← 用户信息卡片
│  └────┘ INTJ · 技术从业者        │     点击进入编辑
│        完善你的画像 →            │
│                                 │
├─────────────────────────────────┤
│                                 │
│  📊 阅读统计                     │  ← 统计（从首页移过来）
│  累计阅读 12 本 · 阅读时长 48h   │     可展开看详细
│  本月 3 本 · 本周 2h            │
│                                 │
├─────────────────────────────────┤
│                                 │
│  功能入口                       │  ← 列表
│  ─────────────────────────     │
│  📖 阅读记录             →      │     历史记录列表
│  📝 我的评论             →      │     发出的评论
│  👥 关注/粉丝            →      │     社交关注列表
│  🔔 通知                 →      │     通知列表（有红点）
│  ⚙️ 设置                 →      │     主题/字体/缓存
│                                 │
├─────────────────────────────────┤
│                                 │
│  [退出登录]                     │  ← 底部红色文字按钮
│                                 │
└─────────────────────────────────┘
```

#### 组件拆分

```
pages/profile/index.tsx                   ← 页面编排（~60 行）
components/profile/UserInfoCard.tsx        ← 用户信息 + 画像引导
components/profile/ReadingStatsCard.tsx    ← 阅读统计（可展开）
components/profile/MenuList.tsx            ← 功能入口列表
components/profile/MenuItem.tsx            ← 单个菜单项
```

---

### 4.7 注册 / Onboarding

#### 注册流程

```
步骤 1：输入邮箱 → 步骤 2：验证码 → 步骤 3：设置密码 → 完成注册
```

不收集任何画像信息。注册即完成，直接进入首页。

#### Onboarding 流程

注册完成后，在首页展示温和的引导，**不阻塞用户使用**：

```
┌─────────────────────────────────┐
│                                 │
│  🎉 欢迎来到 KBook！             │
│                                 │
│  告诉我一些你的偏好，             │  ← 半屏弹窗（可关闭）
│  我帮你找到最适合的书             │
│                                 │
│  你的年龄段？                    │
│  [18-24] [25-34] [35-44] [45+]  │  ← 单选
│                                 │
│  你感兴趣的领域？（可多选）        │
│  [科技] [商业] [心理] [历史]      │  ← 多选标签
│  [文学] [哲学] [科幻] [传记]      │
│  [自我提升] [理财] [健康]         │
│                                 │
│  [跳过]            [下一步 →]    │
│                                 │
└─────────────────────────────────┘
```

第二步（选填，在个人中心触发）：

```
┌─────────────────────────────────┐
│  完善画像                       │
│                                 │
│  以下信息会帮助 AI 更好地为你     │
│  推荐书籍，非必填                │
│                                 │
│  MBTI 类型                      │
│  [INTJ] [INTP] ...              │  ← 下拉或 tag 选择
│                                 │
│  职业领域                       │
│  [技术/研发] [产品] [设计] ...   │
│                                 │
│  一句话介绍自己                  │
│  ┌─────────────────────────┐    │
│  │ "喜欢系统思维的技术人"    │    │  ← 自由文本
│  └─────────────────────────┘    │
│                                 │
│  [保存]                         │
│                                 │
└─────────────────────────────────┘
```

#### 关键设计决策

- **注册不收集画像**——每一步额外表单会降低约 20% 的注册转化率。注册只需邮箱+密码+验证码
- **欢迎引导可跳过**——注册后弹出半屏引导，只收集 4 项：生日、MBTI、职业、孩子年龄。其余字段在个人中心补充
- **心情是动态维度**——在首页推荐区域顶部放置 emoji 横滑条（😊 😢 😡 😰 😌），用户随时点击切换当前心情，触发推荐列表异步重算
- **孩子从 boolean 变为区间多选**——`0-2岁` / `3-6岁` / `7-12岁` / `13-17岁` / `18+岁` / `无孩子`，算法用邻近衰减
- **收入/学历统一为 aspiration 语义**——前端 label 保持「期望年收入」「期望学历」，后端字段名同步改为 `aspirationIncome` / `aspirationEducation`，向 AI 打分 prompt 传递时明确标注为 aspiration
- **画像价值显性化**——在推荐模块旁标注"基于你的画像"，点击跳转个人资料页

---

## 五、核心操作流程

### 5.1 新用户首次体验

```
注册完成
   │
   ▼
进入首页
   │
   ├─→ 首页弹出 Onboarding 弹窗（可关闭）
   │      │
   │      ├─→ 填写 → 立即生成个性化推荐
   │      │
   │      └─→ 跳过 → 看到默认热门推荐
   │
   ▼
浏览首页「发现」Tab
   │
   ├─→ 点击一本书 → 进入详情页
   │      │
   │      ├─→ 看三分钟速读（决策是否值得读）
   │      │      │
   │      │      ├─→ 感兴趣 → 加入书架 → 开始阅读
   │      │      │
   │      │      └─→ 不感兴趣 → 返回首页
   │      │
   │      └─→ 看到「完善画像后获取个性化推荐」
   │             │
   │             └─→ 去个人中心完善画像 → 首页推荐更新
   │
   ▼
开始阅读一本书
   │
   ├─→ 遇到不懂的 → 长按选中文本 → AI 解释
   ├─→ 读完一章 → 自动保存进度
   └─→ 读完 → 弹出评分 + 简短评价引导
```

### 5.2 找书→决策→阅读

```
首页
   │
   ├─→ 搜索书名
   │      │
   │      └─→ 搜索结果列表 → 点击 → 详情页
   │
   ├─→「为你推荐」
   │      │
   │      └─→ 点击推荐书 → 详情页
   │
   └─→「发现」Tab
          │
          └─→ 点击书 → 详情页

详情页（决策页）
   │
   ├─→ 第一眼：封面 + 书名 + 评分（2 秒判断是否继续看）
   │
   ├─→ 往下滑：三分钟速读（15-30 秒决策核心）
   │      │
   │      ├─→ 核心观点：讲什么
   │      ├─→ 适合谁 / 不适合谁：与我相关吗
   │      ├─→ 你将收获：读完后我能得到什么
   │      └─→ 为什么适合你（个性化）：为什么推荐给我
   │
   ├─→ [开始阅读] → 进入阅读器
   │
   └─→ [加入书架] → 稍后读

阅读器
   │
   ├─→ 沉浸式阅读
   ├─→ 长按文本 → AI 解释/总结
   ├─→ 右下角 AI 按钮 → 打开对话
   └─→ 退出时自动保存进度
```

### 5.3 AI 深度阅读

```
入口 1：详情页 → 预设问题 → 发送 → BookChatSheet
入口 2：详情页 → 右下角悬浮按钮 → BookChatSheet
入口 3：阅读器 → 长按选中 → AI 解释
入口 4：阅读器 → 右下角悬浮按钮 → BookChatSheet
入口 5：底栏 AI Tab → 独立 AI 对话页
```

AI 对话的核心场景：

| 场景 | 触发方式 | AI 行为 |
|------|---------|--------|
| 了解一本书 | 详情页预设问题 | 回答基于 RAG 检索的书中内容 |
| 解释段落 | 阅读器长按选中文本 | 用通俗语言解释选中文本 |
| 总结章节 | 阅读器长按选中 → AI 总结本章 | 生成当前章节的结构化摘要 |
| 个性化咨询 | AI 对话页自由提问 | 结合用户画像给出个性化回答 |
| 书籍对比 | AI 对话页"对比 A 和 B" | 从多维度对比两本书 |
| 推荐下一本 | AI 对话页"推荐类似的书" | 基于当前书 + 用户画像推荐 |

### 5.4 阅读→反馈→推荐迭代

```
用户读完一本书（进度 ≥ 90%）
   │
   ▼
弹出轻量反馈
   │
   ├─→ ⭐ 评分（1-5 星）
   ├─→ 👍/👎 推荐准确吗？（仅对个性化推荐的书）
   └─→ 可选：一句话评价
          │
          ▼
      反馈写入用户画像权重
          │
          ├─→ 高评分 → 同类书权重 +5%
          ├─→ 低评分 → 同类书权重 -10%
          ├─→ "推荐准确" → 当前推荐策略权重 +1%
          └─→ "不感兴趣" → 当前推荐策略权重 -2%，触发画像微调引导
```

---

## 六、工程改进清单

### 6.1 立即执行（P0 — 本周）

| 序号 | 行动 | 文件/范围 | 原因 |
|------|------|----------|------|
| 1 | 所有 `catch { /* ignore */ }` 加 `console.error` + 用户可感知降级 | 35+ 处 | 沉默失败 = 用户体验黑洞 |
| 2 | `book/detail.tsx` 拆分为 ≤200 行组件 | `frontend/src/pages/book/detail.tsx` | 1266 行不可维护 |
| 3 | `BookChatSheet.tsx` 拆分为 ≤200 行组件 | `frontend/src/components/book/BookChatSheet.tsx` | 692 行不可维护 |
| 4 | `useEpubReader.ts` 拆分逻辑和渲染 | `frontend/src/hooks/useEpubReader.ts` | 641 行 hook，职责太多 |
| 5 | 阅读器三 hook 抽象公共接口 `ReaderAdapter` | `frontend/src/hooks/use*Reader.ts` | 当前格式间行为不一致 |
| 6 | 前端补 5 个核心测试 | `frontend/src/` | 零测试 → 无法重构 |

### 6.2 短期执行（P1 — 两周内）

| 序号 | 行动 | 范围 |
|------|------|------|
| 1 | API 类型安全：消除所有 `as any` 类型断言 | 全量 API 调用处 |
| 2 | 首页模块从 8 个精简到 4 个 | `frontend/src/pages/home/` |
| 3 | 三分钟速读提升到详情页首屏主体 | `frontend/src/pages/book/detail.tsx` |
| 4 | 书页详情页匹配度默认折叠 | 同上 |
| 5 | 书架空状态 + 长按菜单 | `frontend/src/pages/bookshelf/` |
| 6 | 阅读器长按文本菜单（AI 解释 + 复制） | `frontend/src/components/reader/` |
| 7 | 注册流程简化：去除画像收集步骤 | 注册页面 + 后端 API |
| 8 | Onboarding 流程：渐进式弹窗引导 | `frontend/src/components/onboarding/` |
| 9 | 后端 `findAll()` 全量加载改为分页/向量预过滤 | `RecommendService.java` |
| 10 | 孩子维度：`hasChildren: Boolean` → `childrenAgeRanges: String`（多选区间） | `User` entity + `RecommendMatchCalculator` + 前端表单 + AI 打分 prompt |
| 11 | 收入/学历语义统一：字段名改为 `aspirationIncome` / `aspirationEducation` | `User` entity + 前端 label + AI 打分 prompt |
| 12 | 心情快捷切换：首页 emoji 横滑条 | `frontend/src/components/home/MoodQuickSwitch.tsx` |
| 13 | 覆盖率系数最低值从 0.42 提升到 0.6 | `RecommendMatchCalculator.java` |

### 6.3 中期执行（P2 — 一个月内）

| 序号 | 行动 | 范围 |
|------|------|------|
| 1 | 错误处理策略：全局 ErrorBoundary + 分类 error toast | 前端全局 |
| 2 | 骨架屏系统：首页/详情页/书架独立骨架 | 前端各页面 |
| 3 | 书籍引用卡片（AI 回复中可点击跳转） | `BookChatSheet` + 独立 AI 页 |
| 4 | AI 追问建议基于用户画像个性化 | 后端 `BookChatService` |
| 5 | 速读「为什么适合你」段落个性化生成 | 后端 `BookParserService` |
| 6 | 推荐反馈闭环：读完评分 → 推荐权重调整 | 后端 `RecommendService` |
| 7 | 埋点体系：页面浏览/点击/转化漏斗 | 前后端 |
| 8 | 后端测试覆盖核心服务（RecommendService/BookChatService/AuthService） | 后端 |

---

## 七、执行路线图

### Phase 1：止血（Week 1）

目标：代码可以安全重构

- [ ] 修复所有 `catch { /* ignore */ }`（至少加日志 + 降级 UI）
- [ ] 拆 `book/detail.tsx`（1266 → 多组件）
- [ ] 拆 `BookChatSheet.tsx`（692 → 多组件）
- [ ] 补 5 个前端核心测试 + 后端核心测试
- [ ] 定义 `ReaderAdapter` 接口

### Phase 2：首页 + 详情页重构（Week 1-2）

目标：核心页面达到设计方案标准

- [ ] 首页精简到 4 模块
- [ ] 详情页速读提升到首屏
- [ ] 匹配度默认折叠
- [ ] 书架空状态 + 长按菜单

### Phase 3：AI + 阅读器深耕（Week 2-3）

目标：差异化功能打磨

- [ ] 阅读器长按文本菜单
- [ ] 三 reader hook 统一为 ReaderAdapter 模式
- [ ] AI 书籍引用卡片
- [ ] Onboarding 渐进式引导

### Phase 4：画像优化 + 数据闭环（Week 3-4）

目标：推荐质量可衡量，画像字段粒度优化

- [ ] 孩子维度：boolean → 年龄区间（后端 entity + 算法 + AI prompt）
- [ ] 收入/学历：字段改为 aspiration 语义（entity 改名 + AI prompt 标注）
- [ ] 心情快捷切换：首页 emoji 横滑条 UI
- [ ] 覆盖率系数最低值 0.42 → 0.6
- [ ] 推荐反馈闭环
- [ ] 埋点体系
- [ ] 后端性能优化（findAll → 向量预过滤）

---

## 附录 A：当前代码质量快照

| 指标 | 当前值 | 目标值 |
|------|--------|--------|
| 最大文件行数（前端） | 1266 行 | ≤ 200 行 |
| 最大文件行数（后端） | 1862 行 | ≤ 400 行 |
| 空 catch 块（前端） | 35+ 处 | 0 处 |
| 前端测试文件 | 0 | ≥ 10 |
| 后端测试文件 | 0 | ≥ 10 |
| `as any` 使用次数 | 50+ 处 | 0 处（除第三方库适配） |
| 首页模块数 | 8 | 4 |
| 用户画像字段数 | 10 维度 | 10 维度（粒度优化：孩子 boolean→区间，收入/学历改名，心情加快捷入口） |

## 附录 B：组件命名规范

所有组件遵循以下命名规则：

```
pages/{domain}/index.tsx            ← 页面编排（只负责组合子组件）
components/{domain}/{Name}.tsx      ← 业务组件
components/ui/{name}.tsx            ← shadcn/ui 基础组件
hooks/use{Domain}{Feature}.ts       ← 自定义 Hook
store/{domain}.ts                   ← Zustand Store
api/{domain}.ts                     ← API 函数（一个领域一个文件）
types/{domain}.ts                   ← 类型定义
```

---

> 本文档是产品评审和重构方案，不是需求文档。所有设计细节在进入开发前需与前后端负责人对齐。
> 核心原则：**少即是多，内容优先，反馈即时，一致性**。
